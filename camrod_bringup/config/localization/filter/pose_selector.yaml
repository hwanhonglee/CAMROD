/**:
  ros__parameters:
    primary_pose_cov_topic: "/localization/primary/pose_with_covariance"
    primary_odom_topic: "/localization/primary/odometry"

    fallback_pose_cov_topic: "/localization/kimera_vio/pose_with_covariance"
    fallback_odom_topic: "/localization/kimera_vio/odometry"

    mode_topic: "/localization/mode"

    selected_pose_topic: "/localization/pose"
    selected_pose_cov_topic: "/localization/pose_with_covariance"
    selected_odom_topic: "/localization/odometry/filtered"
    selected_source_topic: "/localization/pose_source"

    primary_source_label: "primary_filter"
    fallback_source_label: "kimera_vio"

    base_frame_id: "robot_base_link"

    # Selector Publish Final Result reference TF 
    publish_selected_tf: true

    # LocalizationMode: NORMAL=0, DEGRADED=1, DR_ONLY=2, INVALID=3
    fallback_on_mode_at_or_above: 2

    primary_timeout_sec: 0.5
    fallback_timeout_sec: 0.5
    switch_hysteresis_sec: 0.5

    publish_localization_status: false
    localization_status_topic: "/localization/status"

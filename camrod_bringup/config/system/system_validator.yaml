# 2026-02-02 10:32 Sync from package config into bringup/config
# HH_260112 Default system validator configuration.
/system/system_validator:
  ros__parameters:
    check_period_s: 1.0
    required_nodes:
      - /map/cost_grid/lanelet
      - /planning/goal_snapper
      - /planning/planner_server
      - /planning/controller_server
      - /planning/bt_navigator
      - /localization/ekf_filter
    required_topics:
      - /localization/pose
      - /sensing/imu/data
      - /sensing/gnss/ublox_gps_node/fix
      - /platform/wheel/odometry
      - /localization/vio/odometry
      - /map/cost_grid/lanelet

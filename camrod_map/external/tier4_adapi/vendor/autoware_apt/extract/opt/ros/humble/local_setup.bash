#!/usr/bin/env bash
# HH_260408: synthetic local_setup for deb-extracted prefix.
# The actual environment extension is handled by camrod_map/external/tier4_adapi/vendor/autoware_apt/setup.bash.
return 0 2>/dev/null || exit 0

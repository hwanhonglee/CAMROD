# HH_260617: Required by ros2 launch Node(package=camrod_parking, executable=...)
# so console scripts install into install/camrod_parking/lib/camrod_parking.
[develop]
script_dir=$base/lib/camrod_parking

[install]
install_scripts=$base/lib/camrod_parking

# camrod_common

Facade package that re-exports interfaces from `avg_msgs` so downstream code can include/import
`camrod_common/msg/...` and `camrod_common/action/...` in a nav2_msgs-like package style.
